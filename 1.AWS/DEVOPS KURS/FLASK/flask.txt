1.Cloud'dan calistirma:
  a.terminal acilir:
    >sudo yum update -
    >sudo yum install python3 -y
    >python3 --version
    >sudo pip3 install flask
    >pip3 list
    >sudo yum install git wget
    >wget URL(raw)
    >sudo python3 app.py
  b.ec2:
    -Details > Public IPv4 address

